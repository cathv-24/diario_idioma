// Entorno de PRODUCCIÓN (GitHub Pages).
// No hay backend público, así que apiUrl queda vacío y la app funciona 100%
// en el navegador con el corrector local (modo demo).
// Si algún día hospedas el backend, pon aquí su URL (ej: 'https://tu-api.com/api').
export const environment = {
  produccion: true,
  apiUrl: '',
};
